import React, { FC } from 'react';

interface GestionTiendasProps {
  
}

const GestionTiendas: FC<GestionTiendasProps> = ({ }) => {
  return (
    <div>GestionTiendas</div>
  );
};

export default GestionTiendas;