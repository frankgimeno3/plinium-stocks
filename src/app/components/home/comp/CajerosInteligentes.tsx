import React, { FC } from 'react';

interface CajerosInteligentesProps {
  
}

const CajerosInteligentes: FC<CajerosInteligentesProps> = ({ }) => {
  return (
    <div>CajerosInteligentes</div>
  );
};

export default CajerosInteligentes;